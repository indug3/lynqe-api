name: CI/CD Pipeline

on:
  push:
    branches:
      - '**'  
  pull_request:
    branches:
      - '**'  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Set Environment Variables
        run: echo "NODE_ENV=production" >> $GITHUB_ENV

      - name: Package the Application
        run: tar -czf backend-build.tar.gz src/server package.json package-lock.json

      - name: Upload Package as an Artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: backend-build.tar.gz

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Download the built package
        uses: actions/download-artifact@v4
        with:
          name: backend-build

      - name: Extract package
        run: tar -xzf backend-build.tar.gz

      - name: Deploy Application
        run: |
          echo "Deploying application..."
          # Add actual deployment commands here (e.g., SSH to a server, copy files, restart services)
